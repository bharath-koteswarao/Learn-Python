# or

print(2 & 3)

# or

print(10 | 20)

"""
Right shift operator shifts the k rightmost bits
n >> k means n/(2^k)
"""

print(10 >> 1)

"""
Left shift operator shifts the k rightmost bits
n >> k means n*(2^k)
"""
print(10 << 1)

# !, ^(XOR) are some other operators

# Python doesn't have ++ -- operators
